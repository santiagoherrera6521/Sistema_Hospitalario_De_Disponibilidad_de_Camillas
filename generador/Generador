#include <iostream>
#include <string>
#include <thread>
#include <chrono>

int main() {
    while (true) {
        std::string id = "PX-00341";
        std::cout << "Enviando: " << id << "\n";

        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
    return 0;
}